# Test Performance Report

The pass rate reached 100% on attempt 2.

```mermaid
xychart-beta
    title "QC Test Pass Rate by Attempt"
    x-axis "Attempt" [1, 2]
    y-axis "Pass Rate (%)" 0 --> 100
    line "Pass Rate" [0, 100]
```
